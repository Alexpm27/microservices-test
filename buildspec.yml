version: 0.2

phases:
  runtime-versions:
    java: corretto17
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean install
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/test-service.jar
  discard-paths: yes